spring:
  application:
    name: service-test1
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: CONFIG-SERVER
server:
  port: 10010
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/